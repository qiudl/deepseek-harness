/** Dependency-free session lease filename shared by readers and the native writer lock. */

/** Base name of the kernel lock file inside a session's directory. */
export const LEASE_FILENAME = 'session.lock'
